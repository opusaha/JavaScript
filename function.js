  // for add 2 number:
  function add(x,y) {
    var result = x+y;
    document.write("add = " + result + "<br>");
    }
    add(10,20);
  
      // for sub 2 number:
      function sub(x,y) {
        var result = x-y;
        document.write("sub = " + result + "<br>");
      }
      sub(50,25);
    
      // for multi 2 number:
      function multi(x,y){
        var result=x*y;
        document.write("multi = " + result + "<br>");
      }
      multi(5,10);
      // for devi 2 number:
      function devi(x,y) {
        var result = x/y;
        document.write("dev = " + result + "<br>");     
      }
      devi(60,2)
  
      // for function mudul:
      function modul(x,y){
        var result = x%y;
        document.write (result);
      }
      modul(15,4)